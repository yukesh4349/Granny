{
  "name": "elderly-ai-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Comprehensive AI-powered elderly companion monorepo",
  "workspaces": [
    "apps/*",
    "backend",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace=@elderly-ai/web",
    "dev:mobile": "npm run start --workspace=@elderly-ai/mobile",
    "dev:backend": "npm run start:dev --workspace=backend",
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test:suites",
    "test:suites": "npx tsx tests/e2e-integration.test.ts && npx tsx tests/sync-contract.test.ts && npx tsx tests/system-smoke.test.ts"
  },
  "overrides": {
    "react-native": "0.74.5",
    "react": "18.2.0"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
